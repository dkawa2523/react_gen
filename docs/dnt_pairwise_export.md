# DNT pair-wise export

The normalized pair-wise DNT+/DNT+DM input export format is defined in [DNT+/DNT+DM input export](dnt_input_export.md).

Keep this page as a short compatibility pointer so older references to `dnt_pairwise_export.md` do not describe a stale output layout.

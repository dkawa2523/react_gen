# Ar/O2 Simple Benchmark Fixture

This fixture is synthetic/minimal and for workflow benchmark only.

Do not use these values for production plasma modeling. Replace `internal_data/`
and `cross_sections/` with reviewed internal data, licensed public snapshots, or
reviewed literature data before scientific use.

The curated `registry/` is not mutated by these fixtures. Benchmark commands write
only to the configured workspace and output directories.
